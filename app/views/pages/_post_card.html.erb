<div class="card-product">
  <%= link_to post_path(post) do %>
    <%= cl_image_tag post.photo.key, height: 800, width: 800, crop: :fill %>
    <div class="card-product-infos">
      <h2><%=post.title%></h2>
      <p><%=truncate(post.content, length: 150)%></p>
    <% end %>
    </div>
</div>
