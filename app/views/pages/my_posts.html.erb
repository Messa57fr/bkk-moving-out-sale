<div class = container>
  <h1>You have <%= Post.count %> Posts</h1>
  <div class = "cards">
    <% @my_posts.each do |post|%>
      <%= render 'post_card', post: post %>
    <% end %>
  </div>
</div>
